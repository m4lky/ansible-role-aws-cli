# Playbook to run aws cli install role
#
# Example command
# ansible-playbook ansible-role-aws-cli/tests/test.yml --extra vars "key_id=" "access_key="

- hosts: localhost
  connection: local
  become: true
  roles:
   - ansible-role-samba
